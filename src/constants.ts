import type { GameConstants } from "./types";

export const CONSTANTS: GameConstants = {
  BIRD_SIZE: 40,
  PIPE_WIDTH: 60,
  PIPE_GAP: 180,
  GRAVITY: 0.5,
  JUMP_STRENGTH: -9,
  GAME_HEIGHT: 600,
  GAME_WIDTH: 400,
  PIPE_SPEED: 3,
  PIPE_INTERVAL: 2000
};